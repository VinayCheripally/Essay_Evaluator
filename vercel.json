{
  "builds": [
    {
      "src": "googlelogin/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "googlelogin/wsgi.py"
    }
  ],
  "functions": {
    "/(.*)": {
      "runtime": "python3.9",
      "includeFiles": "requirements.txt",
      "command": "pip install --upgrade pip && pip install --disable-pip-version-check --target . --upgrade -r /vercel/path0/requirements.txt"
    }
  }
}
